rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Tree post images — authenticated users can upload, anyone can read
    match /trees/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }

  }
}
